#pragma once

#include "structures/voxel/spk_voxel_shape.hpp"

namespace pg
{
	// Atlas coordinate shared with the spk voxel shapes (column + row into the texture atlas).
	using AtlasCell = spk::AtlasCell;
}
